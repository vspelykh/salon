/**
 * This package contains the service layer of the application, which provides the business logic and acts as an
 * interface between the presentation layer and the data layer.
 * It contains interfaces and classes that define the services offered by the application and their implementations.
 * The services are responsible for performing the necessary operations on the domain objects,
 * such as creating, reading, updating and deleting records from the database.
 * The services use DAO objects to interact with the database and perform transactions using the Transaction object.
 * <p>
 * The EmailService class in this package is a utility class that provides a method to send emails.
 */
package ua.vspelykh.salon.service;